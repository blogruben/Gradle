plugins {
    id 'java'
    id 'application'
}

application {
    //clase principal para "gradlew -q run"
    mainClassName = 'org.ruben.gradle.idiomas.App'
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

task wrapVersion(type: Wrapper) {
   gradleVersion = '5.6' 
}